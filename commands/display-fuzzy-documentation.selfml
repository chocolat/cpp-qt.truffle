(trigger
    (key [[ctrl][alt][cmd]H]))

(name [Display Fuzzy Documentation])

(only-in source.c++.qt)

(input selection word)

(output html)

(save nothing)

(script [ruby "$TM_BUNDLE_SUPPORT/bin/search.rb" fuzzy input])