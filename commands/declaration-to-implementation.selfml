(trigger
    (key [[cmd]U]))

(name [Declaration to Implementation])

(only-in source.c++.qt)

(input selection line)

(output snippet)

(save nothing)

(script [# Converts method declarations to their
# out-of-class-declaration implementations.
#
# Could be applied to bunch of methods in
# selection.

ruby "$TM_BUNDLE_SUPPORT/bin/declaration_to_implementation.rb"])