(trigger
    (key [[shift][cmd]R]))

(name [Build & Run])

(only-in [source.c++.qt, source.qmake])

(input nothing)

(output html)

(save nothing)

(script [#!/bin/sh
"${TM_BUNDLE_SUPPORT}/bin/build_qt_project.rb" && "${TM_BUNDLE_SUPPORT}/bin/run_qt_project.rb"])